// useWebSocket — custom hook for WebSocket management.
// This is a placeholder file to establish the directory structure.
// Implementation will be added when real-time features are built.
//
// Future API:
//   const { sendMessage, lastMessage, readyState } = useWebSocket('/ws')

export { }
